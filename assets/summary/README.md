# Summary screenshots

Store the screenshots used by the root README Summary in this directory.
Capture real project resources and workflows rather than fixtures made only for the screenshot.

| Summary section                       | File                     |
| ------------------------------------- | ------------------------ |
| Read anything through `read`          | `read.png`               |
| Find anything through `search`        | `search.png`             |
| Express edits as intentions           | `editing.png`            |
| Guarded search-and-replace selections | `search-and-replace.png` |
| Persistent terminal interactions      | `terminal.png`           |
| User-facing terminal process view     | `user_terminal.png`      |
| Interactive debugger sessions         | `debugging.png`          |
| Agent IDE settings                    | `settings.png`           |

The root README displays the smaller files in `thumbs/`. Each thumbnail links to the corresponding full-resolution image in this directory.
