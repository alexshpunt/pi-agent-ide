/** Compose the Apply frame with the configured editor's final mutation panel. */
export { createApplyResultRenderer, type ApplyMutationPanel } from "#src/core/apply/renderer.js";
export { createApplyCallRenderer } from "#src/core/apply/renderer.js";
